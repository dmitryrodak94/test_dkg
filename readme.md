# create venv 
python3.12 -m venv .venv

# on macos/linux
source .venv/bin/activate     

# install package
pip install -r requirements.txt

# run 
python3 main.py

# What you built, in a paragraph.
# Get data from public gsheet sheet -> save data locally -> parser data of websites, 

# About 1h-2h
# Next I would test abilities to add missing data HQ, Founded

# - What would break first if this ran against 20,000 records instead of 12.
# this functionallity allows to easily choose 